# Arsh Portfolio (React + MUI)

## Run locally
```bash
npm install
npm run dev
```

## Add your resume PDF
Put your resume at:
`public/resume.pdf`

Then visit `/resume` route.

## Deploy ideas
- GitHub Pages (via a gh-pages workflow)
- Netlify / Vercel
- AWS S3 + CloudFront
